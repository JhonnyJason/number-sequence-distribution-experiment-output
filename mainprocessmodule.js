// Generated by CoffeeScript 2.5.1
(function() {
  var cfg, compressionTester, distributionTester, log, mainprocessmodule, olog, ostr, print;

  mainprocessmodule = {
    name: "mainprocessmodule"
  };

  //#############################################################################
  //region logPrintFunctions
  log = function(arg) {
    if (allModules.debugmodule.modulesToDebug["mainprocessmodule"] != null) {
      console.log("[mainprocessmodule]: " + arg);
    }
  };

  olog = function(o) {
    return log("\n" + ostr(o));
  };

  ostr = function(o) {
    return JSON.stringify(o, null, 4);
  };

  print = function(arg) {
    return console.log(arg);
  };

  //endregion

  //#############################################################################
  //region modulesFromEnvironment
  distributionTester = null;

  compressionTester = null;

  cfg = null;

  //endregion

  //#############################################################################
  mainprocessmodule.initialize = function() {
    log("mainprocessmodule.initialize");
    distributionTester = allModules.distributiontestmodule;
    compressionTester = allModules.compressiontestmodule;
    cfg = allModules.configmodule;
  };

  
  //#############################################################################
  //region exposedFunctions
  mainprocessmodule.execute = function(args) {
    log("mainprocessmodule.execute");
    // distributionTester.runTest()
    compressionTester.runTest();
  };

  //endregion
  module.exports = mainprocessmodule;

}).call(this);
